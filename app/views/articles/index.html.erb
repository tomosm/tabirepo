<%= stylesheet_link_tag    "top", :media => "all" %>
<!-- <h4>投稿一覧</h4> -->
<section>
  <% @articles.each do |article| %>
    <article class="article">
      <header><%= article.title %></header>
      <div>
        <!-- <%= link_to image_tag(article.photo.url(:thumb), :size => "200x150", :class => 'img-polaroid'), article_path(article) %> -->
        <%= link_to image_tag(article.photo.url(:thumb), :class => 'img-polaroid'), article_path(article) %>
      </div>
      <footer><%= article.description %></footer>
    </article>
  <% end -%>
</section>
